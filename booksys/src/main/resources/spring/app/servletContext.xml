<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd">

	<!-- 配置controller扫描的包 -->
	<context:component-scan base-package="cn.com.dhc.nssol.*.controller" />

	<!-- 配置视图解析器 -->
	<bean id="viewResolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<!-- 配置文件上传解释器 -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="maxUploadSize" value="10485760" />
		<property name="defaultEncoding" value="UTF-8" />
		<property name="maxInMemorySize" value="4096" />
		<property name="uploadTempDir" value="/tempDir/uploadTempDir" />
	</bean>

	<!-- 静态资源文件配置 -->
	<bean id="messageSource"
		class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
		<property name="basenames">
			<list>
				<value>classpath:messagesConf/messages</value>
				<value>classpath:messagesConf/fieldName</value>
			</list>
		</property>
		<property name="fileEncodings" value="UTF-8" />
		<property name="defaultEncoding" value="UTF-8" />
		<property name="cacheSeconds" value="120" />
	</bean>


	<!-- 校验器配置 -->
	<bean id="validator"
		class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean">
		<!-- 错误信息资源 -->
		<property name="validationMessageSource" ref="messageSource" />
		<!-- 校验器 -->
		<property name="providerClass" value="org.hibernate.validator.HibernateValidator" />
	</bean>

	<!-- 请求资源文件请求配置 -->
	<mvc:resources location="/resources/" mapping="/resources/**" />
	<mvc:annotation-driven validator="validator" />

	<mvc:view-controller path="/topPage" view-name="top" />
	<mvc:view-controller path="/leftPage" view-name="left" />
	<mvc:view-controller path="/mainfraPage" view-name="mainfra" />
	<mvc:view-controller path="/loginPage" view-name="login" />
	<mvc:view-controller path="/indexPage" view-name="index" />

	<!-- 拦截器的配置 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<bean class="cn.com.dhc.nssol.web.interceptor.AccessLogInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>

</beans>
